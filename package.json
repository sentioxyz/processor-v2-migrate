{
  "name": "migrate",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "sentio test",
    "gen": "sentio gen",
    "build": "sentio build",
    "upload": "sentio upload",
    "postinstall": "sentio gen"
  },
  "dependencies": {
    "@sentio/sdk": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^4.7.2",
    "@sentio/cli": "^2.0.0"
  }
}